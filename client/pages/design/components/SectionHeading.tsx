import { H, HLevel } from 'react-h-tag'
import { fontSizes, primaryFont } from '~/client/styles/typography'
import styled from '@emotion/styled'

export const SectionHeading = styled(H)`
  font-size: ${fontSizes.h4};
`
